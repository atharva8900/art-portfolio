export const metadata = {
    title: "Commission Process | Atharva Sherlekar Art",
    description:
        "A detailed step-by-step guide to how a custom portrait commission works — from filling out the form to receiving your artwork at your door.",
};

export default function CommissionProcessLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
